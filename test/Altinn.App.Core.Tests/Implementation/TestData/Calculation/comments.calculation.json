{
    "$schema": "https://altinncdn.no/toolkits/altinn-app-frontend/4/schemas/json/calculation/calculation.schema.v1.json",
    // Comments were accepted by the previous parser, and are common in hand-edited app config
    "calculations": [
        {
            "field": "skjema.name",
            /* A literal value is also a valid expression */
            "expression": "fixed"
        }
    ]
}
